<div class="navbar-wagon">
  <!-- Logo -->
  <a href="/" class="navbar-wagon-brand">
    <img src="http://www.clker.com/cliparts/1/e/r/C/F/f/chef-hat.svg" alt="logo">
  </a>
  <!-- Right Navigation -->
  <div class="navbar-wagon-right">

 <%= link_to'about', about_path %>
    <% if user_signed_in? %>
    <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
    <% else %>
    <%= link_to "Log in", new_user_session_path %>
    <% end %>

    <!-- Profile picture with dropdown list -->
    <div class="navbar-wagon-item">
      <div class="dropdown">
        <% if user_signed_in? %>
        <%= cl_image_tag current_user.avatar, height: 300, width: 400, crop: :fill, crop: :thumb, gravity: :face, :class => "avatar dropdown-toggle", :id => "navbar-wagon-menu", data: {toggle: "dropdown"} %>
        <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
          <li> <%= link_to "Mes reservations", bookings_path %> </li>
          <li> <%= link_to "Mes services reservés", my_bookings_path %> </li>
          <li> <%= link_to "Modifier mon profil", edit_user_registration_path %> </li>
        </ul>
        <% end %>
      </div>
    </div>
  </div>
</div>
